import "package:flutter/material.dart";

const PrimaryColor = Color.fromRGBO(231, 247, 250, 1);
const PrimaryColorTransparent = Color.fromRGBO(231, 247, 250, 0.1);
const PrimaryColorBlue = Color.fromRGBO(30, 80, 128, 1);
const PrimaryWhite = Colors.white;

// Fichier pour les couleurs de l'application
